// Full thick-spring female with a flat outer face and local hinge relief.

use <../../openscad/sliding-dovetail/sliding_dovetail_render.scad>

$vpt = [20, 2.4, 0];
$vpr = [72, 0, 35];
$vpd = 62;

sliding_dovetail_hinge_design(view = "overview");
